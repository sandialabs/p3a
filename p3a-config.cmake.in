# Compute paths
get_filename_component(p3a_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

set(p3a_ENABLE_AVX512 @p3a_ENABLE_AVX512@)
set(p3a_ENABLE_CUDA @p3a_ENABLE_CUDA@)
set(p3a_ENABLE_HIP @p3a_ENABLE_HIP@)
set(p3a_LANGUAGE @p3a_LANGUAGE@)

# Our library dependencies (contains definitions for IMPORTED targets)
if(NOT TARGET p3a AND NOT p3a_BINARY_DIR)
  include("${p3a_CMAKE_DIR}/p3a-targets.cmake")
endif()
